#include<iostream>
#include<vector>
using namespace std;
int SOS(int,int);
int arr[200],n,cap;

int main(){
    cin >> n;
    for(int i=0;i<n;i++)
       cin >> arr[i];
    return 0;
}

int SOS(int temp,int n){
   if( temp )
   return SOS(int )
}